#!/bin/bash

ARG1="$1"
RESTVAR="${*:2}"
RESTVAR_ESC="${RESTVAR/\"/\\\"}"

# echo $ARG1
# echo $RESTVAR
# echo $RESTVAR_ESC

mvn me.andrz:maven-executable:executable "-Dartifact=${ARG1}" "-Darguments=${RESTVAR_ESC}"
